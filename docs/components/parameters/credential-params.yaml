credential_id:
  in: path
  name: credential_id
  description: Credential identifier
  required: true
  schema:
    type: string
workspace_id:
  in: query
  name: workspace_id
  description: Workspace identifier
  required: true
  schema:
    type: string
filter[component]	:
  in: query
  name: filter[component]
  description: Only credentials belong to the specified component
  schema:
    type: string
